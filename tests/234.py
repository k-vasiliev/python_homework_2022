"""str = "The following example creates an ArrayList with a capacity of 50 elements.\
        Four elements are then added to the ArrayList and the ArrayList is trimmed accordingly."

for w in str.split():  # пройтись по каждому слову
    if (w.startswith("a") or w.startswith("e") or w.startswith("A") or w.startswith("E")):
        print(w, end=', ')  # ,то вывести его
        """
